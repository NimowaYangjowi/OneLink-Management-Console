/**
 * Settings route page for managing template IDs and attribution presets.
 */
import OneLinkSettingsPage from '@/components/onelink/OneLinkSettingsPage';

export default function SettingsPage() {
  return <OneLinkSettingsPage />;
}
