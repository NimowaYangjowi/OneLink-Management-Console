# src/app/create/single-link

Route segment for the single OneLink creation screen.

## Files

- `page.tsx` - Renders the existing create UI for single-link flow.
- `[id]/page.tsx` - Renders the same UI in edit mode for an existing single-link record.
