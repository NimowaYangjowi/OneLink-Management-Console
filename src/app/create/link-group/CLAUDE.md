# src/app/create/link-group

Route segment for the link-group creation screen.

## Files

- `page.tsx` - Renders the Phase A tree-based Link Group creation flow.
- `[id]/page.tsx` - Redirects legacy edit links to `/link-groups/:id`.
