# src/app/create

Route segment for creation flow selection and nested create flows.

## Files

- `page.tsx` - Renders the create-type chooser (single link vs link group).
- `single-link/page.tsx` - Renders the existing create UI for single-link flow.
- `link-group/page.tsx` - Renders the existing create UI for link-group flow.
