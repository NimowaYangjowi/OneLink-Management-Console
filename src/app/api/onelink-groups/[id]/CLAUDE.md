# src/app/api/onelink-groups/[id]

Dynamic API segment for operating on a single link group.

## Files

- `route.ts` - returns detail and handles delete modes.
- `retry/route.ts` - retries failed items in the group.
