# src/app/api

Route handlers for server-side APIs consumed by client components.

## Files

- `settings/route.ts` - Read/write endpoint for persisted console settings in SQLite
- `onelinks/route.ts` - Create/list endpoint for generated OneLink records
