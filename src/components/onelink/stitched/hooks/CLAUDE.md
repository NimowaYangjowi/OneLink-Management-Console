# stitched/hooks

- This folder contains custom hooks that encapsulate OneLink stitched-page state,
  validation rules, derived values, and API side effects.
- Keep render-free orchestration logic in hooks so page components remain focused
  on layout composition.
