# src/components/navigation

Navigation components for the dashboard sidebar system.

## Files

- `SidebarContext.tsx` - React context + provider for sidebar collapsed/expanded state
- `SidebarItem.tsx` - Individual navigation link item with icon, label, and active state
- `DashboardSidebar.tsx` - Full sidebar component with logo, nav items, and collapse toggle
