# src/components/layout

Layout components for page structure and dashboard shell.

## Files

- `DashboardLayout.tsx` - Top-level dashboard layout combining SidebarProvider + DashboardSidebar + main content area
